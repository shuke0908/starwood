const fs = require('fs');
const path = require('path');

// This is a simplified version to simulate large data in memory for now
// Or we can modify the settings.svelte.ts directly or a mock file.
// Let's modify the mock-data.ts if it exists or create a large dataset file.
